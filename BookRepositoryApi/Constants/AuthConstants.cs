namespace BookRepositoryApi.Constants;

// Stores authentication-related constants used by the application.
public static class AuthConstants
{
    // The access token lifetime in minutes.
    public const int AccessTokenLifetimeMinutes = 60;
}

